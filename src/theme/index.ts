export const theme = {
  colors: {
    primary: "hsl(var(--primary))",
    destructive: "hsl(var(--destructive))",
    muted: "hsl(var(--muted))",
  },
  spacing: {
    page: "1rem",
    section: "1.5rem",
  },
} as const;
